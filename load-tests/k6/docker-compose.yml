version: "3.9"

services:
  k6:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      K6_TARGET_URL: ${K6_TARGET_URL:-http://host.docker.internal:8080/health/ready}
      K6_VUS: ${K6_VUS:-10}
      K6_DURATION: ${K6_DURATION:-30s}
