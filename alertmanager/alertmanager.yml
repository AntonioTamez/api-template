global:
  resolve_timeout: 5m

route:
  receiver: 'default'
  group_by: ['alertname']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: 'default'
    # Add your own notification integrations below. Examples:
    # email_configs:
    #   - to: ops@example.com
    #     from: alerts@example.com
    #     smarthost: smtp.example.com:587
    #     auth_username: ${ALERTMANAGER_SMTP_USER}
    #     auth_password: ${ALERTMANAGER_SMTP_PASSWORD}
    # webhook_configs:
    #   - url: http://webhook-listener:8080/alert
